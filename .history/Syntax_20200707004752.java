public class Syntax {
    /* All java methods and functions runs under class, and first Character must be Capital.
    Name of the class should match with the filename. */
    public static void main(String[] args) {
    /* The first method in class is the main, which is the entrance of the method.
    other entrance could be __init__ which identify the object content. */
        // There is multi-type of variable we can use: String, 
        // type variable = value;
        System.out.println("Hello World");    /* Instead of print(), Java using System.out.println()
        Java using ";" as end of line, not "\n". Use "{}" to make blocks of code. */

    }
  }